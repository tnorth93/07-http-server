'use strict';

const superagent = require('superagent');

describe('#server.test.js', () => {
  test('POST request should respond with 200 and body if no error', () => {
  });
  test('POST should respond with 400 if there is an error', () => {
  });
});
